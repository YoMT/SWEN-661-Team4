// Stub for static asset imports (images/fonts) so Jest can parse modules that
// `import x from './foo.jpg'`. Vite resolves these to a URL string at build time;
// under Jest they just need to be a harmless string.
module.exports = 'test-file-stub'
